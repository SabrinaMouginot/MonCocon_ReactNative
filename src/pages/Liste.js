import React from 'react';
import { View, Text } from 'react-native';

const Liste = ({ navigation }) => {
    return (
        <View>
            <Text>Liste</Text>
        </View>
    );
};

export default Liste;
