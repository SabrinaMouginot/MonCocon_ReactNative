import React from 'react';
import { View, Text } from 'react-native';

const GestionnaireListe = ({ navigation }) => {
    return (
        <View>
            <Text>Chambre</Text>
        </View>
    );
};

export default GestionnaireListe;
